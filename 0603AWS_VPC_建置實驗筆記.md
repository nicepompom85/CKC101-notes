# AWS VPC 建置實驗筆記

> CKC101 課程實作 · VPC / Subnet / Route Table / IGW

---

## 一、實驗目標與架構概覽

本次實驗目標是在 AWS 上手動建立一個完整的 VPC 網路環境，包含公有與私有子網路，並正確設定 Internet Gateway 與路由表，讓公有子網路可以對外上網，私有子網路則保持隔離。

| 項目 | 設定值 |
|---|---|
| VPC 名稱 | ckc023 |
| VPC CIDR | 10.23.0.0/16 |
| 區域（Region） | ap-east-2（亞太 — 台灣） |
| 可用區域 | ap-east-2a、ap-east-2b（各放一組子網路） |
| 公有子網路 1 | public-1　10.23.16.0/24　(ap-east-2a) |
| 公有子網路 2 | public-2　10.23.32.0/24　(ap-east-2b) |
| 私有子網路 1 | private-1　10.23.10.0/24　(ap-east-2a) |
| 私有子網路 2 | private-2　10.23.20.0/24　(ap-east-2b) |
| 路由表（公有） | 主要路由表（自動產生，綁定 IGW） |
| 路由表（私有） | private-rt（手動建立，僅 Local 路由） |
| Internet Gateway | ckc023-igw（Attach 到 ckc023 VPC） |

> 💡 **注意：** 所有 CIDR 在設計時應確保子網路不重疊，且 VPC CIDR 要能涵蓋所有子網路範圍。

---

## 二、完整建置步驟

### 步驟一　建立 VPC

VPC（Virtual Private Cloud）是所有 AWS 資源的網路基礎，第一步先建立自訂的隔離網路。

1. 進入 AWS Console → 搜尋並點選「VPC」服務
2. 左側選單點選「您的 VPC」，右上角按「建立 VPC」
3. 選擇建立選項：**僅限 VPC**（只建 VPC，不含子網路）
4. 輸入名稱標籤：`ckc023`
5. IPv4 CIDR 區塊：手動輸入 `10.23.0.0/16`（可容納 65,536 個 IP 位址）
6. IPv6 CIDR 區塊：不選（本實驗使用純 IPv4）
7. 租用方式：**預設（Default）**
8. 點選「建立 VPC」完成建立

> 💡 **注意：** CIDR /16 表示前 16 位元為網路位元，可分配的主機位址從 10.23.0.1 到 10.23.255.254。建立後 VPC ID 會以 `vpc-` 開頭。

---

### 步驟二　建立 Internet Gateway（IGW）並 Attach 到 VPC

Internet Gateway 是 VPC 對外連接 Internet 的唯一出入口，沒有 IGW，公有子網路的資源就無法被外部存取，也無法主動連線到外部網路。

1. 左側選單點選「**Internet 閘道**」
2. 右上角按「建立 Internet 閘道」
3. 輸入名稱標籤：`ckc023-igw`
4. 點選「建立 Internet 閘道」
5. 建立後會看到 IGW 狀態為「**已中斷連接（Detached）**」
6. 點選右上角「動作」→「**連接至 VPC**」
7. 在下拉選單選擇剛建立的 `ckc023` VPC
8. 點選「連接 Internet 閘道」
9. 完成後狀態變更為「**已連接（Attached）**」

> 💡 **注意：** 一個 VPC 只能 Attach 一個 IGW。若先前已有 IGW 連接到此 VPC，需先 Detach 才能連接新的。

---

### 步驟三　建立公有子網路 public-1（ap-east-2a）

子網路（Subnet）是 VPC 內的 IP 位址區段，公有子網路表示其路由表有指向 Internet Gateway 的路由規則，讓資源可以對外連線。本實驗要在不同的可用區域（AZ）各建立一個，提升可用性。

1. 左側選單點選「**子網路**」，右上角按「建立子網路」
2. VPC ID：選擇 `ckc023`（vpc-04b8336f95c936dc7）
3. 子網路名稱：`public-1`
4. 可用區域：`ap-east-2a`
5. IPv4 子網路 CIDR 區塊：`10.23.16.0/24`（可用 IP 254 個）
6. 點選「**新增子網路**」繼續設定第二個子網路

---

### 步驟四　建立公有子網路 public-2（ap-east-2b）

（接續上一步，同一個建立頁面）

1. 子網路名稱：`public-2`
2. 可用區域：`ap-east-2b`（切換到**不同的 AZ**）
3. IPv4 子網路 CIDR 區塊：`10.23.32.0/24`
4. 點選「**建立子網路**」完成建立兩個公有子網路

> 💡 **注意：** 此時子網路還不算「公有」，需要後續在路由表加入指向 IGW 的路由，並啟用「自動指派公有 IP」才算真正對外可用。  
> 啟用方式：選取子網路 → 動作 → 編輯子網路設定 → 勾選「**啟用自動指派公有 IPv4 地址**」

---

### 步驟五　建立私有路由表（private-rt）

路由表（Route Table）告訴 VPC 流量要往哪裡送。公有子網路會使用含有 IGW 路由的路由表；私有子網路則使用只有 Local 路由的路由表，確保私有子網路的流量不會直接到達 Internet。

1. 左側選單點選「**路由表**」，右上角按「建立路由表」
2. 名稱標籤：`private-rt`
3. VPC：選擇 `ckc023`
4. 點選「建立路由表」
5. 建立後會自動有一條 Local 路由規則：`10.23.0.0/16 → local`（VPC 內部通訊）
6. 確認路由表中**沒有** `0.0.0.0/0 → IGW` 的路由（這是私有路由表的關鍵）

> 💡 **注意：** 公有路由表（主要路由表）需要額外設定：選取主要路由表 → 路由 → 編輯路由 → 新增路由：**目的地 `0.0.0.0/0`，目標 `ckc023-igw`**，這樣公有子網路才能對外連線。

---

### 步驟六　建立私有子網路 private-1（ap-east-2a）

私有子網路內的資源（如資料庫、後端 App Server）不需要直接對外，因此關聯只有 Local 路由的 private-rt，確保隔離性。

1. 左側選單點選「**子網路**」，右上角按「建立子網路」
2. VPC ID：選擇 `ckc023`
3. 子網路名稱：`private-1`
4. 可用區域：`ap-east-2a`（與 public-1 同一個 AZ）
5. IPv4 子網路 CIDR 區塊：`10.23.10.0/24`
6. 點選「**新增子網路**」繼續

---

### 步驟七　建立私有子網路 private-2（ap-east-2b）

（接續，同一個建立頁面）

1. 子網路名稱：`private-2`
2. 可用區域：`ap-east-2b`（與 public-2 同一個 AZ）
3. IPv4 子網路 CIDR 區塊：`10.23.20.0/24`
4. 點選「**建立子網路**」完成建立

---

### 步驟八　將私有子網路關聯到 private-rt

1. 左側選單點選「**路由表**」，選取 `private-rt`（rtb-0a4d0581282f7c0da）
2. 下方點選「**子網路關聯**」標籤頁
3. 按「**編輯子網路關聯**」
4. 勾選 `private-1` 和 `private-2`
5. 點選「**儲存關聯**」
6. 完成後「明確子網路關聯」會顯示 private-1 和 private-2

> 💡 **注意：** 若子網路沒有明確關聯任何路由表，會自動歸屬到 VPC 的「主要路由表」。為了清楚管理，私有子網路要明確關聯 private-rt，避免誤用含 IGW 的公有路由表。

---

## 三、子網路配置總覽

| 名稱 | 類型 | CIDR | 可用區域 | 路由表 |
|---|---|---|---|---|
| public-1  | 公有 | 10.23.16.0/24 | ap-east-2a | 主要路由表（含 IGW） |
| public-2  | 公有 | 10.23.32.0/24 | ap-east-2b | 主要路由表（含 IGW） |
| private-1 | 私有 | 10.23.10.0/24 | ap-east-2a | private-rt（僅 Local） |
| private-2 | 私有 | 10.23.20.0/24 | ap-east-2b | private-rt（僅 Local） |

---

## 四、路由表規則說明

### 4.1 主要路由表（公有子網路使用）

| 目的地（Destination） | 目標（Target） | 說明 |
|---|---|---|
| 10.23.0.0/16 | local | VPC 內部流量互通 |
| 0.0.0.0/0 | ckc023-igw | 所有對外流量走 IGW，實現公有子網路上網 |

### 4.2 private-rt（私有子網路使用）

| 目的地（Destination） | 目標（Target） | 說明 |
|---|---|---|
| 10.23.0.0/16 | local | VPC 內部流量互通（不對外） |

---

## 五、常見問題與注意事項

- **子網路 CIDR 不可重疊：** 同一個 VPC 內的子網路 CIDR 必須互相不重疊，否則路由會產生衝突。
- **IGW 一對一：** 一個 VPC 只能連接一個 Internet Gateway。
- **主路由表自動套用：** 未明確關聯路由表的子網路，自動使用 VPC 的主要路由表，需小心避免私有子網路意外走到含 IGW 的路由表。
- **自動指派公有 IP：** 子網路本身設定為「啟用自動指派公有 IPv4 位址」後，該子網路內建立的 EC2 才會自動獲得公有 IP，否則只有私有 IP。
- **跨 AZ 設計：** 公有與私有子網路各自分布在兩個 AZ，是 AWS 高可用性設計的基本原則，可避免單點 AZ 故障影響服務。
- **私有子網路出網需求：** 若私有子網路的資源需要主動連線到 Internet（如下載套件更新），需額外建立 **NAT Gateway** 放在公有子網路，並在 private-rt 加入 `0.0.0.0/0 → NAT Gateway` 的路由。

---

## 六、完成截圖

### 截圖 1　VPC 資源對應圖（private-rt 路由表）

![VPC 資源對應圖 - private-rt](img1.png)

*圖 1：VPC ckc023 資源對應圖，顯示 private-1 和 private-2 關聯到 private-rt 路由表*

---

### 截圖 2　VPC 資源對應圖（主要路由表）

![VPC 資源對應圖 - 主要路由表](img2.png)

*圖 2：public-1 和 public-2 使用主要路由表（rtb-01bcb9993f2456a54，含 2 條路由含 IGW）*

---

### 截圖 3　路由表詳細設定

![路由表詳細設定](img3.png)

*圖 3：private-rt 路由表詳情，明確子網路關聯為 private-1（10.23.10.0/24）和 private-2（10.23.20.0/24）*

---

### 截圖 4　VPC 詳細資訊

![VPC 詳細資訊](img4.png)

*圖 4：ckc023 VPC 詳細資訊，CIDR 10.23.0.0/16，DNS 解析已啟用，狀態 Available*

---

*CKC101 課程筆記 | AWS VPC 建置實驗*
