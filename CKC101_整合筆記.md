# 雲端網路應用開發課程筆記

**CKC101 課堂實作記錄**

> **【實作】** = 課堂原始筆記　　**📌 補充說明** = 額外補充內容

---

## 一、安裝開發環境

**【實作】** 老師讓我們安裝了 Antigravity IDE

📌 **補充說明**

Antigravity IDE 是基於 VS Code 架構的整合開發環境（IDE）。IDE 全名為 Integrated Development Environment，將程式碼編輯器、終端機、檔案管理與除錯工具整合在同一個視窗，讓開發流程更有效率。

Antigravity IDE 右側內建 AI 助理（Gemini 3.5 Flash），可以直接在 IDE 內詢問程式問題，不需要另外切換視窗。

IDE 與 AI 的差異：IDE 是寫程式的工具（像一個專業工作桌）；AI 是能對話回答問題的語言模型（像一個助理）。Antigravity IDE 把兩者結合，讓你邊寫邊問。

---

## 二、建立專案資料夾

**【實作】** 老師要我們做專案資料夾，裡面通常會有 SRC 跟 TEST 資料夾

📌 **補充說明**

標準 Python 專案將程式碼分為兩個主要資料夾：

- **SRC（Source）資料夾**：存放所有正式的功能程式碼，包含主程式、路由定義、設定檔等。這是專案的核心，交付給別人時需要包含此資料夾。
- **TEST 資料夾**：存放測試程式碼，用來驗證 SRC 內的程式是否正確運作。測試碼與功能碼分離，有助於維護，交付時通常不需要包含。

---

**【實作】** 在 Antigravity IDE 建造專案資料夾

📌 **補充說明**

在 Antigravity IDE 中建立資料夾有兩種方式：

- 方式一：點首頁藍色「Open Folder」按鈕，選擇已在電腦建好的資料夾。
- 方式二：開啟 Terminal（上方選單 Terminal → New Terminal），輸入以下指令一次建好：

```bash
mkdir -p MyProject/src MyProject/test
```

也可以在左側檔案欄對著專案名稱右鍵 → 選「New Folder」逐一建立。

---

## 三、Flask 應用程式開發

**【實作】** 在 Antigravity IDE 下指令：幫我寫一個 Python 的 Flask 應用，部署在 19191 port，程式碼用 SRC 資料夾跟 TEST 資料夾做切割

📌 **補充說明**

Flask 是 Python 的輕量級網頁框架，適合快速建立 API 或小型網站。AI 依照指示將程式碼依照 SRC 與 TEST 資料夾切割，並設定監聽埠號為 19191。

生成的專案結構如下：

- `src/app.py`：Flask 主程式，使用 Application Factory 模式，透過 `create_app()` 建立應用實例。
- `src/routes.py`：路由定義，負責將 URL 對應到處理函式。
- `test/test_app.py`、`test/test_routes.py`：對應的測試程式碼。
- `requirements.txt`：記錄專案使用的套件清單。

啟動時需要在專案根目錄建立 `run.py` 作為入口，內容為呼叫 `create_app()` 並設定 `host='0.0.0.0', port=19191`。

---

**【實作】** 接著再下指令：幫我繼續追加兩個功能

**【實作】** 用戶訪問 feature1 的時候，會看見早上要看股票

**【實作】** 用戶訪問 feature2 的時候，會看見要找下午上班的公司

📌 **補充說明**

路由（Route）是 Flask 中將 URL 對應到處理函式的機制，定義在 `src/routes.py` 中：

- `/feature1`：用戶訪問此路徑時，頁面顯示「早上要看股票」。
- `/feature2`：用戶訪問此路徑時，頁面顯示「要找下午上班的公司」。

新增路由後需確認 `app.py` 有透過 `register_blueprint` 將路由註冊進去，才能正確對外提供服務。

---

**【實作】** URL 跟 URN 是什麼

📌 **補充說明**

URL 與 URN 都是 URI（Uniform Resource Identifier，統一資源識別符）的子集：

| 名稱 | 說明 | 範例 |
|------|------|------|
| **URL** | 用「位置」定位資源，資源搬走後 URL 會失效 | `https://example.com/page` |
| **URN** | 用「名稱」永久識別資源，不含位置資訊 | `urn:isbn:978-3-16-148410-0` |

---

## 四、Git 版本控制與 GitHub 上傳

**【實作】** 接著要上傳到 GitHub，下載安裝 Git，GitHub 註冊帳號

📌 **補充說明**

Git 是一套分散式版本控制系統，可記錄每次程式碼的修改歷程，方便回溯與多人協作。執行 `git init` 後，Git 會在專案資料夾內建立隱藏的 `.git` 資料夾管理版本記錄，請勿手動刪除。

在 Antigravity IDE 初始化 Git 後，左側檔案欄的檔名旁會出現 `U`（Untracked）標記，代表這些檔案尚未被 Git 追蹤。

---

**【實作】** 執行以下 Git 指令完成上傳：

**設定身份（第一次使用 Git 才需要）：**

```bash
git config --global user.name "nicepompom85"
```
設定 Git 使用者名稱，對應 GitHub 帳號。

```bash
git config --global user.email "racing18028@gmail.com"
```
設定 Git 使用者信箱，需與 GitHub 帳號的 Email 一致。

**加入暫存區與建立版本：**

```bash
git add .
```
將所有檔案加入暫存區（Staging Area），準備進行 commit。

```bash
git commit -m "hello"
```
正式建立一個版本記錄。`-m` 是 message 的縮寫，讓我們直接在指令後附上說明文字，不需要開啟文字編輯器。

**連接 GitHub 並上傳：**

```bash
git branch -M main
```
將預設分支名稱重新命名為 main，符合 GitHub 的現行標準。

```bash
git remote add origin https://github.com/nicepompom85/flask.git
```
告訴本地 Git 遠端倉庫的位置，origin 是遠端的代稱。

```bash
git push -u origin main
```
將本地的 commit 推送到 GitHub。`-u` 設定預設推送目標，之後修改完只需執行 `git push` 即可。

---

## 五、AWS 雲端服務設定

**【實作】** 開始使用 AWS，申請帳號（需填信用卡）

📌 **補充說明**

前往 https://aws.amazon.com 申請帳號，需填寫信用卡資訊以驗證身份。AWS 提供免費方案（Free Tier），每月有一定的免費使用額度，例如 EC2 t2.micro 免費使用 750 小時。建議申請完後立即進行以下安全設定。

---

**【實作】** 開啟登入 MFA 功能

📌 **補充說明**

MFA（Multi-Factor Authentication，多因素驗證）是除了密碼之外，還需要第二種驗證方式才能登入的安全機制。常見方式為手機 App（如 Google Authenticator）產生的 6 位數動態碼。

Root 帳號權限最高，務必開啟 MFA。一旦 Root 帳號被盜，整個 AWS 資源都可能遭到濫用，後果非常嚴重。

---

**【實作】** 開啟預算功能

📌 **補充說明**

AWS 本身無法強制停止超支，但可透過 AWS Budgets 設定每月預算上限並發送警告通知。

設定路徑：AWS Console 搜尋欄輸入「Budgets」→ Create budget → 選擇 Cost budget → 設定月預算金額 → 在 Alert threshold 設定 80% 時寄送 Email 警告。

---

**【實作】** AWS Organization

📌 **補充說明**

AWS Organizations 是 AWS 提供的多帳號管理服務，讓企業或團隊可以用一個主帳號（Management Account）統一管理多個子帳號。其層級結構如下：

- **Organization Root**：整個組織的最頂層容器。
- **Management Account**：主帳號，擁有最高管理權限，負責統一結帳與套用政策。此帳號一定要開 MFA。
- **OU（Organizational Unit）**：部門分組，例如開發部門、營運部門，方便統一管理一批帳號。
- **Member Account**：各部門實際使用的子帳號，資源互相隔離，費用自動匯總至主帳號結帳。

SCP（Service Control Policy）可從上層統一限制所有子帳號能使用的 AWS 服務，例如強制開啟 MFA 或禁止使用特定地區的服務。

---

**【實作】** IAM 的群組建立

📌 **補充說明**

IAM（Identity and Access Management）用於管理 AWS 使用者帳號與存取權限，本次課堂建立了以下架構：

- **Admin 群組**：附加 AdministratorAccess 政策，擁有完整管理權限，使用者為個人帳號（nicepompom85）。
- **Develop 群組**：不附加任何政策，目前無任何存取權限，使用者為 DEV_nicepompom85。

驗證方式：分別以兩個帳號登入，嘗試進入 EC2 或 S3 服務。Admin 帳號應可正常存取；Develop 帳號應顯示「You don't have permissions」錯誤，代表權限設定正確。

IAM 登入時需要填入三個欄位：AWS Account ID（12 位數字）、IAM username、以及密碼。Account ID 可在 IAM Dashboard 頁面上方找到。

---

## 六、EC2 Windows Server 部署

**【實作】** 要在 AWS 租一台 Windows Server，確認 3389 port 有開，讓這台使用「ckc101」當 key pair

📌 **補充說明**

在 AWS EC2 建立 Windows Server 的重要設定：

- **Key Pair**：使用 ckc101，建立時會下載 `ckc101.pem` 檔案，請妥善保存，遺失後無法重新下載，且無法取得 Windows 登入密碼。
- **AMI（作業系統映像）**：選擇 Microsoft Windows Server 2022 Base。
- **Instance type**：選擇 t2.micro（符合免費方案）。
- **Security Group Inbound rules**：需開放 RDP 3389 埠（用於遠端桌面連線）。

建立完成後，需在 AWS Console 透過 Actions → Security → Get Windows password，上傳 `ckc101.pem` 後解密取得 Administrator 密碼，Windows Server 第一次啟動約需等待 4 分鐘才能解密。

---

**【實作】** 用這台 Windows Server 拉 GitHub 的原始碼

📌 **補充說明**

透過 RDP（遠端桌面連線）進入 Windows Server 後，進行以下步驟：

- 安裝 Git：前往 https://git-scm.com/download/win 下載並安裝，全程保持預設值。
- 開啟 cmd 確認安裝成功：`git --version`
- 切換到目標目錄並執行 clone：

```bash
cd C:\
mkdir Projects
cd Projects
git clone https://github.com/nicepompom85/flask.git
```

注意：GitHub 現在不接受直接用密碼登入，需要使用 Personal Access Token（PAT）。在 GitHub Settings → Developer settings → Personal access tokens 產生 Token，clone 時密碼欄位貼上此 Token 即可。

---

**【實作】** 在 AWS Windows Server 上執行 Flask 專案

📌 **補充說明**

部署步驟：

- 安裝 Python：前往 https://python.org/downloads 下載，安裝時務必勾選「Add Python to PATH」，否則 cmd 找不到 pip 與 python 指令。
- 安裝套件（若 pip 無法使用，改用 python -m pip）：

```bash
python -m pip install -r requirements.txt
```

- 由於專案使用 Application Factory 模式，需在專案根目錄建立 `run.py`：

```python
from src.app import create_app

app = create_app()

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=19191, debug=True)
```

- 啟動 Flask：

```bash
python run.py
```

- 在 AWS Security Group 新增 Inbound rule：Custom TCP，Port 19191，Source 0.0.0.0/0。
- 在 Windows Server 防火牆也需開放 19191：

```bash
netsh advfirewall firewall add rule name="Flask 19191" dir=in action=allow protocol=TCP localport=19191
```

部署成功後，在本機瀏覽器輸入以下網址即可存取：

```
http://EC2的PublicIP:19191
http://EC2的PublicIP:19191/feature1
http://EC2的PublicIP:19191/feature2
```

---

**【實作】** 測試完成後將機器終止

📌 **補充說明**

依需求選擇適當的停止方式：

- **Stop（暫停）**：保留所有資料與設定，之後可重新啟動，但硬碟（EBS）費用仍會計費。適合課程尚未結束、之後可能還要使用的情況。
- **Terminate（終止）**：完全刪除執行個體與所有資料，不再收取任何費用，但無法復原。適合作業已交完、確定不再需要的情況。

學生帳號建議課程結束後執行 Terminate，避免免費方案的 750 小時額度提前用完而產生意外費用。
